<!DOCTYPE html>
<html>
    <head>
        <title>Favr Login</title>
    </head>
    <body>
        <h1>Favr Fam</h1>
        <form name="login">
            Username <input type="text" name="userid" required/> <br><br>
            Password <input type="password" name="pswrd" required/> <br> <br>
            <input type="button" onclick="check(this.form)" value="Login"/> 

            <input type="button" onclick="location.href='signup'" value="Sign-Up"/>
        </form>
        <script language="javascript">
            function check(form) { /*function to check userid & password*/
                /*the following code checkes whether the entered userid and password are matching*/
                if(form.userid.value == "Cam Newton" && form.pswrd.value == "panthers") {
                    window.open('home', '_self',false)/*opens the target page while Id & password matches*/
                }
                else {
                    alert("Incorrect Username or Password")/*displays error message*/
                }
            }
        </script>
    </body>
</html>
